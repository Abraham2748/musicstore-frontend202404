import { HighlightableDirective } from './highlightable.directive';

describe('HighlightableDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightableDirective();
    expect(directive).toBeTruthy();
  });
});
